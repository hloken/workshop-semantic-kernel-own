{
  "schema": 1,
  "type": "completion",
  "description": "Gets the intent of the user",
  "input_variables": [
    {
      "Name": "userRequest",
      "Description": "This is the request from the user",
      "required": true
    }
  ]
}
